import CoreGraphics

/// 4-base spacing scale per DESIGN.md §4.1. Use these tokens for
/// padding, gap, and inter-row spacing so screens stay on the grid.
public enum UnisonSpacing {
    public static let xs: CGFloat = 4
    public static let s: CGFloat = 8
    public static let m: CGFloat = 12
    public static let l: CGFloat = 16
    public static let xl: CGFloat = 24
}
